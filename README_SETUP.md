# Women's Wellness Feed - React Native App

## Setup Instructions

1. Make sure you have React Native development environment set up:
   - Node.js (v14 or higher)
   - Xcode (for iOS)
   - Android Studio (for Android)

2. Install pods for iOS:
   ```bash
   cd ios && pod install && cd ..
